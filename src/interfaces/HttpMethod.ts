/**
 * The HTTP methods available for the authentication and authorization endpoints
 * within the `AuthZ` application.
 */
type HttpMethod = 'GET' | 'POST' | 'PUT' | 'PATCH' | 'DELETE';

export default HttpMethod;
